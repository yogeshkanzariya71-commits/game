<!DOCTYPE html>
<html><head>
<meta charset="UTF-8">
<style>
@import url('https://fonts.googleapis.com/css2?family=Creepster&family=Special+Elite&display=swap');
*{box-sizing:border-box;margin:0;padding:0;}
body{font-family:'Special Elite',cursive;background:#080810;color:#c8b89a;min-height:600px;overflow-x:hidden;}
h1,h2,h3{font-family:'Creepster',cursive;}

/* HUD */
#hud{background:#0d0b0a;border-bottom:1px solid #2a1f15;padding:8px 14px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:6px;}
.hud-cell{text-align:center;}
.hud-lbl{font-size:9px;color:#5a4a35;letter-spacing:1px;text-transform:uppercase;}
.hud-val{font-size:16px;color:#e8c97a;font-family:'Creepster',cursive;}
#fema-bar{font-size:11px;color:#5588cc;border:1px solid #2a3a5a;padding:3px 8px;background:#08101a;}

/* SCREENS */
.screen{display:none;padding:14px;}
.screen.active{display:block;animation:fi .3s ease;}
@keyframes fi{from{opacity:0;transform:translateY(6px)}to{opacity:1;transform:translateY(0)}}

/* BUTTONS */
.btn{font-family:'Special Elite',cursive;background:transparent;border:1px solid #5a4a35;color:#c8b89a;padding:8px 18px;cursor:pointer;font-size:13px;letter-spacing:1px;transition:all .2s;display:inline-block;}
.btn:hover{background:#1e1810;border-color:#8a6a45;color:#e8c97a;}
.btn:active{transform:scale(.97);}
.btn-red{border-color:#7a2a2a;color:#cc5555;}
.btn-red:hover{background:#1a0808;border-color:#cc3333;color:#ff6666;}
.btn-green{border-color:#2a6a3a;color:#55aa66;}
.btn-green:hover{background:#081508;border-color:#44aa55;color:#77cc88;}
.btn-blue{border-color:#2a4a7a;color:#5588cc;}
.btn-blue:hover{background:#080e18;border-color:#4488cc;color:#77aaee;}
.btn-yellow{border-color:#6a5a1a;color:#ccaa44;}
.btn-yellow:hover{background:#11100a;border-color:#aaaa44;color:#eedd66;}
.btn:disabled{opacity:.35;pointer-events:none;}

/* TITLE */
#title-screen{text-align:center;padding:30px 16px;background:radial-gradient(ellipse at center,#160d08 0%,#080810 70%);}
.game-title{font-size:30px;color:#bb3333;letter-spacing:3px;text-shadow:0 0 24px #bb333355;margin-bottom:4px;}
.sub{font-size:11px;color:#6a5a45;letter-spacing:2px;margin-bottom:20px;}
.lore-box{background:#0d0b09;border-left:2px solid #3a2f20;padding:12px 14px;text-align:left;font-size:12px;color:#8a7a65;line-height:1.9;max-width:420px;margin:0 auto 22px;}

/* HOUSE MAP */
#house-screen{padding:10px;}
.map-wrap{display:grid;grid-template-columns:1fr 1fr 1fr;grid-template-rows:auto auto;gap:8px;margin-bottom:10px;}
.room{background:#0d0b09;border:1px solid #2a1f15;padding:10px 8px;text-align:center;cursor:pointer;transition:all .2s;position:relative;min-height:90px;}
.room:hover{border-color:#6a5a35;background:#141008;}
.room.clickable{border-color:#5a4a28;}
.room.full{border-color:#2a5a3a;background:#081308;}
.room.danger{border-color:#2a5a3a;background:#081308;}
.room-name{font-size:10px;color:#5a4a35;letter-spacing:1px;text-transform:uppercase;margin-bottom:6px;}
.room-emoji{font-size:26px;display:block;margin-bottom:4px;}
.occupant{font-size:11px;color:#8a7a65;margin-top:2px;}
.occupant-emoji{font-size:22px;display:block;}
.room-empty{font-size:10px;color:#2a2018;margin-top:6px;}
.room-badge{position:absolute;top:4px;right:4px;font-size:9px;padding:2px 5px;border:1px solid;}
.badge-c{border-color:#1a5a2a;color:#44aa66;background:#081308;}
.badge-h{border-color:#1a5a2a;color:#44aa66;background:#081308;}
.badge-u{border-color:#3a3018;color:#aaaa44;background:#0f0e08;}
.badge-fema{border-color:#2a4a7a;color:#5588cc;background:#08101a;font-size:8px;}

/* NIGHT DOOR */
.door-visitors{display:flex;gap:10px;flex-wrap:wrap;justify-content:center;margin:12px 0;}
.v-card{background:#0d0b09;border:1px solid #2a1f15;padding:12px 10px;width:90px;text-align:center;cursor:pointer;transition:all .2s;position:relative;}
.v-card:hover{border-color:#6a5a35;background:#141008;}
.v-card.let-in{border-color:#2a6a3a;background:#081308;cursor:default;}
.v-card.turned{border-color:#6a2a2a;background:#130808;cursor:default;opacity:.5;}
.v-emoji{font-size:32px;display:block;margin-bottom:6px;}
.v-name{font-size:10px;color:#7a6a55;}
.v-status{font-size:9px;margin-top:4px;padding:2px 4px;}

/* EXAMINE */
.ex-header{display:flex;gap:14px;align-items:center;margin-bottom:14px;padding-bottom:12px;border-bottom:1px solid #1a1510;}
.ex-big{font-size:54px;}
.ex-info h3{font-family:'Creepster',cursive;font-size:20px;color:#e8c97a;}
.ex-info p{font-size:11px;color:#6a5a45;margin-top:2px;}
.exam-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-bottom:14px;}
.exam-tile{background:#0d0b09;border:1px solid #2a1f15;padding:12px;text-align:center;cursor:pointer;transition:all .2s;}
.exam-tile:hover:not(.done){background:#141008;border-color:#6a5a35;}
.exam-tile.done{pointer-events:none;}
.exam-tile.bad{border-color:#cc3333;background:#130808;}
.exam-tile.good{border-color:#2a6a3a;background:#081308;}
.exam-tile.warn{border-color:#6a5a1a;background:#110e05;}
.tile-icon{font-size:22px;display:block;margin-bottom:4px;}
.tile-lbl{font-size:11px;color:#7a6a55;letter-spacing:1px;}
.tile-res{font-size:10px;margin-top:3px;font-weight:bold;}
.res-bad{color:#cc4444;}
.res-good{color:#44aa66;}
.res-warn{color:#ccaa44;}
.charge-note{font-size:11px;color:#5a4a35;background:#0a0905;border:1px solid #1f1a10;padding:7px;margin-bottom:10px;}
.suspect-tags{margin-top:4px;}
.stag{font-size:9px;padding:2px 7px;border:1px solid #6a2a1a;color:#cc5544;background:#130808;display:inline-block;margin:2px;}

/* INFO BOX */
.info{background:#0a0905;border:1px solid #1f1a10;padding:10px;font-size:12px;color:#7a6a55;line-height:1.8;margin-bottom:10px;}
.info .w{color:#cc4444;}
.info .g{color:#44aa66;}
.info .y{color:#ccaa44;}
.info .b{color:#5588cc;}

/* FEMA */
.fema-panel{background:#08101a;border:1px solid #1a2a4a;padding:12px;margin-bottom:12px;}
.fema-title{font-family:'Creepster',cursive;font-size:18px;color:#5588cc;margin-bottom:8px;}
.fema-list{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:10px;}
.fema-person{background:#0a0e18;border:1px solid #2a3a5a;padding:8px;text-align:center;cursor:pointer;width:80px;transition:all .2s;}
.fema-person:hover{border-color:#5588cc;background:#0c1222;}
.fema-person.selected{border-color:#88aaee;background:#0f1830;}
.fema-person .fp-emoji{font-size:22px;display:block;}
.fema-person .fp-name{font-size:9px;color:#5a6a8a;margin-top:3px;}
.fema-counter{font-size:22px;color:#e8c97a;font-family:'Creepster',cursive;}

/* LOG */
#log{border-top:1px solid #1a1510;padding:8px 14px;max-height:70px;overflow-y:auto;background:#060608;}
.log-line{font-size:10px;color:#3a3025;line-height:1.7;}
.log-line.imp{color:#7a6a45;}
.log-line.bad{color:#aa3333;}
.log-line.good{color:#337744;}
.log-line.blue{color:#3a6aaa;}

/* RESULT */
#result-screen{text-align:center;padding:40px 16px;}
.res-icon{font-size:64px;display:block;margin-bottom:14px;}
.res-title{font-size:34px;margin-bottom:10px;}
.res-s{color:#44aa66;}
.res-d{color:#cc3333;}
.res-msg{font-size:13px;color:#7a6a65;line-height:1.8;max-width:380px;margin:0 auto 18px;}
.stat-row{display:flex;gap:18px;justify-content:center;font-size:12px;color:#6a5a45;margin-bottom:22px;}

/* utils */
hr.div{border:none;border-top:1px solid #1a1510;margin:10px 0;}
.row{display:flex;gap:8px;align-items:center;flex-wrap:wrap;}
.row.center{justify-content:center;}
.section-title{font-family:'Creepster',cursive;font-size:18px;color:#8a6a45;margin-bottom:10px;}
@keyframes shake{0%,100%{transform:translateX(0)}25%{transform:translateX(-5px)}75%{transform:translateX(5px)}}
.shake{animation:shake .3s ease;}
</style>
</head>
<body>

<div id="hud" style="display:none">
  <div class="hud-cell"><div class="hud-lbl">Night</div><div class="hud-val" id="h-night">1</div></div>
  <div class="hud-cell"><div class="hud-lbl">Day</div><div class="hud-val" id="h-day">1</div></div>
  <div class="hud-cell"><div class="hud-lbl">In House</div><div class="hud-val"><span id="h-pop">0</span>/5</div></div>
  <div class="hud-cell"><div class="hud-lbl">Charges</div><div class="hud-val" id="h-charges">3</div></div>
  <div class="hud-cell"><div id="fema-bar">FEMA in <span id="h-fema">3</span>d</div></div>
  <div class="hud-cell"><div class="hud-lbl">Score</div><div class="hud-val" id="h-score">0</div></div>
</div>

<!-- TITLE -->
<div id="title-screen" class="screen active">
  <div style="text-align:center;padding:30px 16px;background:radial-gradient(ellipse at center,#160d08,#080810);">
    <pre style="font-size:12px;line-height:1.4;color:#3a2a18;display:inline-block;margin-bottom:14px;">  _   _   _   _   _
 / \ / \ / \ / \ / \
( N | O | T | H | U )
 \_/ \_/ \_/ \_/ \_/</pre>
    <h1 class="game-title">NO, I AM NOT HUMAN</h1>
    <div class="sub">— they came from below —</div>
    <div class="lore-box">
      Underground creatures walk among us in bad disguises. You shelter alone — let strangers in, one per room. By day, examine those inside: nails, eyes, teeth, temperature. Kill the creatures. But be careful — every 3 nights <span style="color:#5588cc">FEMA</span> arrives and takes 2 people. If 2 creatures nest together, they kill a human. If no humans remain, you are next.
    </div>
    <button class="btn" onclick="G.start()">Begin →</button>
  </div>
</div>

<!-- HOUSE MAP -->
<div id="house-screen" class="screen">
  <div class="section-title" id="hs-title">Your Shelter — Day 1</div>
  <div class="map-wrap" id="room-map"></div>
  <div class="info" id="hs-info"></div>
  <div class="row center" id="hs-buttons"></div>
</div>

<!-- NIGHT DOOR -->
<div id="night-screen" class="screen">
  <div class="section-title" id="ns-title">Night — Strangers at the Door</div>
  <div class="info" id="ns-info"></div>
  <div class="door-visitors" id="door-visitors"></div>
  <div class="row center" id="ns-buttons"></div>
</div>

<!-- EXAMINE PERSON -->
<div id="examine-screen" class="screen">
  <div class="ex-header">
    <div class="ex-big" id="ex-emoji"></div>
    <div class="ex-info">
      <h3 id="ex-name"></h3>
      <p id="ex-loc"></p>
      <div class="suspect-tags" id="ex-tags"></div>
    </div>
  </div>
  <div class="charge-note" id="ex-charge-note"></div>
  <div class="exam-grid" id="ex-grid"></div>
  <hr class="div">
  <div class="row center">
    <button class="btn btn-red" id="btn-kill" onclick="G.killPerson()">⚔ Kill</button>
    <button class="btn" onclick="G.backToHouse()">← Back</button>
  </div>
</div>

<!-- FEMA SCREEN -->
<div id="fema-screen" class="screen">
  <div class="fema-panel">
    <div class="fema-title">⚑ FEMA Inspection</div>
    <div class="info" id="fema-info"></div>
    <div class="fema-list" id="fema-list"></div>
    <div id="fema-submit-area"></div>
  </div>
</div>

<!-- RESULT -->
<div id="result-screen" class="screen">
  <span class="res-icon" id="r-icon"></span>
  <h2 class="res-title" id="r-title"></h2>
  <p class="res-msg" id="r-msg"></p>
  <div class="stat-row" id="r-stats"></div>
  <button class="btn" onclick="G.start()">Play Again</button>
</div>

<div id="log"></div>

<script>
const NAMES = [
  'Mara','Eldon','Silas','Verna','Clem','Rowan','Hessa','Tull',
  'Davia','Brek','Nola','Oswin','Faye','Gruth','Pell','Wenda',
  'Corin','Ulric','Sable','Dort',

  'Arthur','Emma','Jack','Olivia','Noah','Sophia','Liam','Ava',
  'Mason','Mia','Ethan','Grace','Lucas','Ella','Logan','Chloe',
  'Henry','Zoe','Leo','Ruby','James','Lily','Daniel','Hannah',
  'Samuel','Lucy','Nathan','Isla','Benjamin','Sarah',];
const PEOPLE_EMOJI = [
  '👨','👩','🧑','👴','👵','🧔',
  '👨‍🦱','👩‍🦱','👨‍🦳','👩‍🦳',
  '👨‍🦰','👩‍🦰',
  '👨‍⚕️',
  '👩‍🏫',
  '👨‍🍳',
  '👨‍🌾',
  '👨‍🏭',
  '👩‍🔧',
  '👩‍💼',
  '👨‍💻',
  '👩‍🔬',
  '👩‍🎨',
  '👨‍🚒',
  '👨‍✈️',
  '👨‍🚀',
  '👨‍⚖️',
  '🕵️',
  '🕵️‍♀️',
  '👷',
  '👷‍♀️',
  '👷‍♂️',
  '💂‍♀️',
  '💂‍♂️',
  '🙍',
  '🙎',
];
const ROOMS=[
  {id:'kitchen',name:'Kitchen',icon:'🍳'},
  {id:'room1',name:'Bedroom 1',icon:'🛏'},
  {id:'room2',name:'Bedroom 2',icon:'🛏'},
  {id:'bathroom',name:'Bathroom',icon:'🚿'},
  {id:'storage',name:'Storage',icon:'📦'},
];
const EXAMS=[
  {id:'nails',icon:'💅',lbl:'Nails',
    cBad:'Overgrown & discolored',cWarn:'Unusually long',
    hOk:'Clean, normal length',hFalse:'Slightly long nails'},
  {id:'eyes',icon:'👁️',lbl:'Eyes',
    cBad:'Red veins, no pupil',cWarn:'Faint red tinge',
    hOk:'Clear, normal',hFalse:'Bloodshot — looks tired'},
  {id:'teeth',icon:'🦷',lbl:'Teeth',
    cBad:'Distorted, wrong count',cWarn:'Jagged edges',
    hOk:'Normal alignment',hFalse:'Minor misalignment'},
  {id:'temp',icon:'🌡️',lbl:'Temp',
    cBad:'41°C — burning hot',cWarn:'39°C — very warm',
    hOk:'36.6°C — normal',hFalse:'37.5°C — mild fever'},
];
function rnd(a){return a[Math.floor(Math.random()*a.length)];}
function rndInt(a,b){return a+Math.floor(Math.random()*(b-a+1));}

const G={
  state:{},

  start(){
    this.state={
      day:1,night:0,
      femaCountdown:3,
      score:0,
      charges:3,
      residents:{}, // roomId -> person obj or null
      femaNotice:[], // up to 2 person ids
      examineeRoom:null,
      phase:'house', // house | night | examine | fema | result
      pendingVisitors:[],
      creaturesKilled:0,humansKilled:0,
      log:[],
    };
    ROOMS.forEach(r=>this.state.residents[r.id]=null);
    document.getElementById('hud').style.display='flex';
    this.log('A new night begins. You are alone.','imp');
    this.startNight();
  },

  /* ---- PHASE: NIGHT ---- */
  startNight(){
    this.state.phase='night';
    const n=this.state.night+1;
    this.state.night=n;
    this.state.charges=3;
    this.updateHUD();

    // How many free rooms?
    const freeRooms=ROOMS.filter(r=>!this.state.residents[r.id]).length;
    if(freeRooms===0){
      this.log('House is full. No one else tonight.','imp');
      this.startDay();
      return;
    }

    // Generate visitors: night 1 = 2 guaranteed humans
    let visitors=[];
    if(n===1){
      visitors=[this.makePerson(true),this.makePerson(true)];
    } else {
      const count=Math.min(rndInt(1,3),freeRooms+rndInt(0,1));
      const numC=rndInt(0,Math.min(2,count));
      for(let i=0;i<count;i++) visitors.push(this.makePerson(i>=numC));
      visitors.sort(()=>Math.random()-.5);
    }
    this.state.pendingVisitors=visitors;
    this.renderNightScreen();
  },

  renderNightScreen(){
    this.showScreen('night-screen');
    const n=this.state.night;
    document.getElementById('ns-title').textContent=`Night ${n} — Strangers at the Door`;
    const freeRooms=ROOMS.filter(r=>!this.state.residents[r.id]).length;
    document.getElementById('ns-info').innerHTML=
      `You have <span class="g">${freeRooms}</span> free room(s). Let visitors in or turn them away. `+
      `Each person taken gets a room. <span class="y">You can only let as many in as you have free rooms.</span>`;
    this.renderDoorVisitors();
    this.renderNightButtons();
  },

  renderDoorVisitors(){
    const wrap=document.getElementById('door-visitors');
    wrap.innerHTML='';
    this.state.pendingVisitors.forEach((v,i)=>{
      const d=document.createElement('div');
      d.className='v-card'+(v.admitted===true?' let-in':v.admitted===false?' turned':'');
      d.innerHTML=`<span class="v-emoji">${v.emoji}</span>
        <div class="v-name">${v.name}</div>
        <div class="v-status" style="color:${v.admitted===true?'#44aa66':v.admitted===false?'#cc4444':'#5a4a35'}">
          ${v.admitted===true?'✓ Let In':v.admitted===false?'✗ Turned Away':'Waiting...'}
        </div>`;
      if(v.admitted===null){
        d.onclick=()=>this.toggleAdmit(i);
      }
      wrap.appendChild(d);
    });
  },

  toggleAdmit(i){
    const v=this.state.pendingVisitors[i];
    if(v.admitted!==null) return;
    const freeRooms=ROOMS.filter(r=>!this.state.residents[r.id]).length;
    const alreadyIn=this.state.pendingVisitors.filter(x=>x.admitted===true).length;
    if(alreadyIn>=freeRooms){
      // can't let more in
      document.getElementById('ns-info').innerHTML=`<span class="w">No free rooms! Turn someone away or end the night.</span>`;
      return;
    }
    // Assign to first free room
    const freeRoom=ROOMS.find(r=>!this.state.residents[r.id]);
    v.admitted=true;
    v.room=freeRoom.id;
    this.state.residents[freeRoom.id]=v;
    this.log(`${v.name} admitted to ${freeRoom.name}.`,'imp');
    this.renderDoorVisitors();
    this.renderNightButtons();
  },

  rejectVisitor(i){
    this.state.pendingVisitors[i].admitted=false;
    this.renderDoorVisitors();
    this.renderNightButtons();
  },

  renderNightButtons(){
    const wrap=document.getElementById('ns-buttons');
    wrap.innerHTML='';
    // Reject buttons for pending
    this.state.pendingVisitors.forEach((v,i)=>{
      if(v.admitted===null){
        const b=document.createElement('button');
        b.className='btn btn-red';
        b.textContent=`Turn Away ${v.name.split(' ')[0]}`;
        b.onclick=()=>this.rejectVisitor(i);
        wrap.appendChild(b);
      }
    });
    const allDecided=this.state.pendingVisitors.every(v=>v.admitted!==null);
    if(allDecided){
      const b=document.createElement('button');
      b.className='btn';
      b.textContent='Sleep → Start Day';
      b.onclick=()=>this.startDay();
      wrap.appendChild(b);
    }
  },

  /* ---- PHASE: DAY ---- */
  startDay(){
    this.state.phase='house';
    // Check: 2+ creatures in house attack
    const residents=Object.values(this.state.residents).filter(Boolean);
    const creatures=residents.filter(r=>!r.isHuman);
    const humans=residents.filter(r=>r.isHuman);
    if(creatures.length>=2 && humans.length>0){
      const victim=rnd(humans);
      this.killResident(victim.room,false);
      this.state.humansKilled++;
      this.log(`ATTACK: ${victim.name} was killed by creatures in the night!`,'bad');
    } else if(creatures.length>=2 && humans.length===0){
      this.endGame(false,'The creatures had no humans to prey on — they came for you.');
      return;
    }
    // Check no humans left
    const remaining=Object.values(this.state.residents).filter(Boolean);
    const humansLeft=remaining.filter(r=>r.isHuman);
    if(humansLeft.length===0 && remaining.length>0){
      this.endGame(false,'No humans remain in your shelter. The creatures turn to you.');
      return;
    }
    this.state.day=this.state.night;
    this.updateHUD();
    this.renderHouseScreen();
  },

  killResident(roomId,byPlayer=true){
    const p=this.state.residents[roomId];
    if(!p) return;
    if(byPlayer){
      if(!p.isHuman) this.state.creaturesKilled++;
      else this.state.humansKilled++;
      this.state.score += p.isHuman ? -30 : 25;
    }
    this.state.residents[roomId]=null;
    this.log(`${p.name} was ${byPlayer?'killed':'eliminated'}.`, byPlayer&&!p.isHuman?'good':'bad');
  },

  /* ---- HOUSE SCREEN ---- */
  renderHouseScreen(){
    this.showScreen('house-screen');
    document.getElementById('hs-title').textContent=`Day ${this.state.day} — Your Shelter`;
    this.renderRoomMap();
    const residents=Object.values(this.state.residents).filter(Boolean);
    const creatures=residents.filter(r=>!r.isHuman);
    let info='Click a room with someone inside to <b>examine</b> them.<br>';
    if(this.state.femaNotice.length>0){
      const names=this.state.femaNotice.map(id=>{
        for(const r of Object.values(this.state.residents)){if(r&&r.id===id)return r.name;}
        return '?';
      }).join(', ');
      info+=`<span class="b">⚑ FEMA Notice issued to: ${names}</span><br>`;
    }
    info+=`<span class="y">FEMA arrives in ${this.state.femaCountdown} night(s).</span>`;
    document.getElementById('hs-info').innerHTML=info;
    // Buttons
    const wrap=document.getElementById('hs-buttons');
    wrap.innerHTML='';
    // FEMA notice button
    const fb=document.createElement('button');
    fb.className='btn btn-blue';
    fb.textContent='Issue FEMA Notice';
    fb.onclick=()=>this.openFemaNotice();
    wrap.appendChild(fb);
    // Night button
    const nb=document.createElement('button');
    nb.className='btn';
    nb.textContent=`Face Night ${this.state.night+1} →`;
    nb.onclick=()=>this.beforeNight();
    wrap.appendChild(nb);
  },

  renderRoomMap(){
    const map=document.getElementById('room-map');
    map.innerHTML='';
    ROOMS.forEach(r=>{
      const p=this.state.residents[r.id];
      const div=document.createElement('div');
      let cls='room';
      if(p) cls+= p.isHuman?' full':' danger';
      else cls+=' clickable';
      div.className=cls;
      div.onclick=p?()=>this.openExamine(r.id):null;
      if(p&&div.onclick) div.style.cursor='pointer';
      const hasFema=p&&this.state.femaNotice.includes(p.id);
      div.innerHTML=`<div class="room-name">${r.name}</div>
        <span class="room-emoji">${r.icon}</span>
        ${p?`<span class="occupant-emoji">${p.emoji}</span>
          <div class="occupant">${p.name}</div>
          <span class="room-badge ${p.isHuman?'badge-h':'badge-c'}">${p.suspicion>=2?'⚠':p.suspicion===1?'~':'✓'}</span>
          ${hasFema?'<span class="room-badge badge-fema" style="top:18px">FEMA</span>':''}
        `:`<div class="room-empty">empty</div>`}`;
      map.appendChild(div);
    });
  },

  /* ---- EXAMINE ---- */
  openExamine(roomId){
    const p=this.state.residents[roomId];
    if(!p) return;
    this.state.examineeRoom=roomId;
    this.showScreen('examine-screen');
    document.getElementById('ex-emoji').textContent=p.emoji;
    document.getElementById('ex-name').textContent=p.name;
    document.getElementById('ex-loc').textContent=`In ${ROOMS.find(r=>r.id===roomId).name}`;
    this.renderExamTags(p);
    this.renderExamGrid(p);
    this.updateChargeNote();
  },

  renderExamTags(p){
    const el=document.getElementById('ex-tags');
    const found=EXAMS.filter(e=>p.examined[e.id]===true||p.examined[e.id]==='warn');
    el.innerHTML=found.map(e=>`<span class="stag">${e.lbl}: suspicious</span>`).join('');
  },

  renderExamGrid(p){
    const grid=document.getElementById('ex-grid');
    grid.innerHTML='';
    EXAMS.forEach(e=>{
      const done=e.id in p.examined;
      const res=p.examined[e.id];
      let cls='exam-tile'+(done?res===true?' bad':res==='warn'?' warn':' good':'');
      const tile=document.createElement('div');
      tile.className=cls;
      let resText='',resClass='';
      if(done){
        if(res===true){resText=p.isHuman?e.hFalse:e.cBad;resClass='res-bad';}
        else if(res==='warn'){resText=p.isHuman?e.hFalse:e.cWarn;resClass='res-warn';}
        else{resText=e.hOk;resClass='res-good';}
      }
      tile.innerHTML=`<span class="tile-icon">${e.icon}</span>
        <div class="tile-lbl">${e.lbl}</div>
        <div class="tile-res ${resClass}">${done?resText:(this.state.charges>0?'Click to examine':'No charges')}</div>`;
      if(!done&&this.state.charges>0) tile.onclick=()=>this.runExam(e.id);
      else if(!done) tile.style.opacity='.35';
      grid.appendChild(tile);
    });
  },

  runExam(examId){
    if(this.state.charges<=0) return;
    const p=this.state.residents[this.state.examineeRoom];
    const e=EXAMS.find(x=>x.id===examId);
    const sym=p.symptoms[examId];
    p.examined[examId]=sym;
    if(sym===true) p.suspicion++;
    else if(sym==='warn') p.suspicion+=0.5;
    this.state.charges--;
    this.updateHUD();
    this.updateChargeNote();
    this.renderExamTags(p);
    this.renderExamGrid(p);
    this.log(`Examined ${p.name}'s ${examId}: ${sym?'⚠ SUSPICIOUS':'✓ clean'}`,sym?'bad':'good');
  },

  updateChargeNote(){
    document.getElementById('ex-charge-note').textContent=
      `Examine charges today: ${this.state.charges} remaining. Each exam costs 1 charge.`;
  },

  killPerson(){
    const p=this.state.residents[this.state.examineeRoom];
    if(!p) return;
    const wasCreature=!p.isHuman;
    this.killResident(this.state.examineeRoom,true);
    this.state.examineeRoom=null;
    this.log(`You killed ${p.name}. ${wasCreature?'It was a CREATURE.':'It was a human...'}`,wasCreature?'good':'bad');
    this.backToHouse();
  },

  backToHouse(){
    this.state.examineeRoom=null;
    this.renderHouseScreen();
  },

  /* ---- FEMA ---- */
  openFemaNotice(){
    this.showScreen('fema-screen');
    const residents=Object.values(this.state.residents).filter(Boolean);
    document.getElementById('fema-info').innerHTML=
      `Issue a FEMA notice to up to <b>2 people</b>. When FEMA arrives (in ${this.state.femaCountdown} nights), they take the noticed people. `+
      `If no notice given, FEMA takes 2 random people. If you have fewer than 2 people in the house when FEMA arrives, you are removed and lose.<br>`+
      `<span class="b">Select up to 2 people below. Click again to deselect.</span>`;
    this.renderFemaList();
  },

  renderFemaList(){
    const list=document.getElementById('fema-list');
    list.innerHTML='';
    const residents=Object.values(this.state.residents).filter(Boolean);
    residents.forEach(p=>{
      const sel=this.state.femaNotice.includes(p.id);
      const div=document.createElement('div');
      div.className='fema-person'+(sel?' selected':'');
      div.innerHTML=`<span class="fp-emoji">${p.emoji}</span><div class="fp-name">${p.name}</div>
        ${sel?'<div style="font-size:9px;color:#5588cc;margin-top:2px">✓ Noticed</div>':''}`;
      div.onclick=()=>{
        if(sel){this.state.femaNotice=this.state.femaNotice.filter(id=>id!==p.id);}
        else if(this.state.femaNotice.length<2){this.state.femaNotice.push(p.id);}
        this.renderFemaList();
      };
      list.appendChild(div);
    });
    const area=document.getElementById('fema-submit-area');
    area.innerHTML='';
    const b=document.createElement('button');
    b.className='btn btn-blue';
    b.textContent='Confirm Notice & Return';
    b.onclick=()=>{this.renderHouseScreen();};
    area.appendChild(b);
    if(residents.length===0){
      list.innerHTML='<div style="font-size:12px;color:#5a4a35">Nobody in the shelter.</div>';
    }
  },

  /* ---- PRE-NIGHT: FEMA CHECK ---- */
  beforeNight(){
    this.state.femaCountdown--;
    if(this.state.femaCountdown<=0){
      this.runFema();
    } else {
      this.updateHUD();
      this.startNight();
    }
  },

  runFema(){
    const residents=Object.values(this.state.residents).filter(Boolean);
    if(residents.length<2){
      this.endGame(false,'FEMA arrived and you had fewer than 2 people in the shelter. You were removed.');
      return;
    }
    let taken=[];
    if(this.state.femaNotice.length>=2){
      taken=residents.filter(p=>this.state.femaNotice.includes(p.id)).slice(0,2);
    }
    if(taken.length<2){
      const rest=residents.filter(p=>!this.state.femaNotice.includes(p.id));
      while(taken.length<2&&rest.length>0){
        const idx=rndInt(0,rest.length-1);
        taken.push(rest.splice(idx,1)[0]);
      }
    }
    taken.forEach(p=>{
      for(const rid of Object.keys(this.state.residents)){
        if(this.state.residents[rid]&&this.state.residents[rid].id===p.id){
          this.state.residents[rid]=null;
        }
      }
      this.log(`FEMA took ${p.name}.`,'blue');
    });
    this.state.femaNotice=[];
    this.state.femaCountdown=3;
    this.updateHUD();
    const remaining=Object.values(this.state.residents).filter(Boolean);
    const humansLeft=remaining.filter(r=>r.isHuman);
    if(humansLeft.length===0&&remaining.length>0){
      this.endGame(false,'After FEMA left, no humans remained. The creatures closed in.');
      return;
    }
    this.state.score+=15;
    this.log('FEMA inspection complete.','blue');
    this.startNight();
  },

  /* ---- END GAME ---- */
  endGame(survived,msg){
    this.showScreen('result-screen');
    document.getElementById('hud').style.display='none';
    document.getElementById('r-icon').textContent=survived?'🌅':'💀';
    const t=document.getElementById('r-title');
    t.textContent=survived?'Survived':'Consumed';
    t.className='res-title '+(survived?'res-s':'res-d');
    document.getElementById('r-msg').textContent=msg;
    document.getElementById('r-stats').innerHTML=
      `<span>Day ${this.state.day}</span><span>Score: ${this.state.score}</span>`+
      `<span>Creatures killed: ${this.state.creaturesKilled}</span><span>Humans lost: ${this.state.humansKilled}</span>`;
  },

  /* ---- HELPERS ---- */
  makePerson(isHuman){
    const id=Math.random().toString(36).slice(2,8);
    const name=rnd(NAMES)+' '+String.fromCharCode(65+rndInt(0,25))+'.';
    const emoji=rnd(PEOPLE_EMOJI);
    const symptoms={};
    EXAMS.forEach(e=>{
      if(!isHuman){
        const r=Math.random();
        symptoms[e.id]=r<0.6?true:r<0.8?'warn':false;
      } else {
        const r=Math.random();
        symptoms[e.id]=r<0.12?'warn':false;
      }
    });
    if(!isHuman){
      let count=Object.values(symptoms).filter(v=>v===true).length;
      while(count<2){
        const k=rnd(EXAMS).id;
        symptoms[k]=true;
        count=Object.values(symptoms).filter(v=>v===true).length;
      }
    }
    return {id,name,emoji,isHuman,symptoms,examined:{},suspicion:0,admitted:null,room:null};
  },

  showScreen(id){
    document.querySelectorAll('.screen').forEach(s=>s.classList.remove('active'));
    document.getElementById(id).classList.add('active');
  },

  updateHUD(){
    const s=this.state;
    document.getElementById('h-night').textContent=s.night;
    document.getElementById('h-day').textContent=s.day||1;
    const pop=Object.values(s.residents).filter(Boolean).length;
    document.getElementById('h-pop').textContent=pop;
    document.getElementById('h-charges').textContent=s.charges;
    document.getElementById('h-fema').textContent=s.femaCountdown;
    document.getElementById('h-score').textContent=s.score;
  },

  log(msg,type=''){
    const log=document.getElementById('log');
    const d=document.createElement('div');
    d.className='log-line '+type;
    d.textContent=`D${this.state.day||1}: ${msg}`;
    log.appendChild(d);
    log.scrollTop=log.scrollHeight;
  }
};
</script>
</body></html>
